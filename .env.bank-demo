# Bank Demo Configuration

# Verifier API port
VERIFIER_PORT=3000

# Anvil port (on-chain mode only)
ANVIL_PORT=8545

# Log level
RUST_LOG=info

# Model path (for bank-demo CLI)
MODEL_PATH=examples/bank-demo/models/credit_scoring.json
