version: "3.8"

services:
  enclave:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - MODEL_PATH=/app/model/model.json
      - PORT=8080
      # Use Anvil account 0 for local testing
      - ENCLAVE_PRIVATE_KEY=0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80
    volumes:
      - ./test-model:/app/model:ro
