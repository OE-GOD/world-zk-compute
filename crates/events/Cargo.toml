[package]
name = "tee-events"
version = "0.1.0"
edition = "2021"

[dependencies]
alloy-primitives = "1.0"
alloy-sol-types = "1.0"
serde = { version = "1.0", features = ["derive"] }
hex = "0.4"

[dev-dependencies]
serde_json = "1.0"
