[package]
name = "tee-events"
version = "0.1.0"
edition = "2021"

[dependencies]
alloy-primitives = "1.0"
alloy-sol-types = "1.0"
serde = { workspace = true }
hex.workspace = true

[dev-dependencies]
serde_json.workspace = true
